@model IEnumerable<InvoiceHeader>

@{
    ViewData["Title"] = "Index";
}

<section class="d-flex justify-content-between">
    <h2>Invoice Headers</h2>
    <div>
        <a asp-action="Add" class="btn btn-outline-primary">Add</a>
    </div>
</section>
<hr />
<section>
    <table class="table table-striped table-bordered">
        <thead>
            <tr class="table-secondary">
                <th>Customer Name</th>
                <th>Invoice Date</th>
                <th>Branch Name</th>
                <th>Cashier Name</th>
                <th width="300px"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var InvoiceHeader in Model)
            {
                <tr>
                    <td>@InvoiceHeader.CustomerName</td>
                    <td>@InvoiceHeader.Invoicedate</td>
                    <td>@InvoiceHeader.Branch.BranchName</td>
                    <td>@InvoiceHeader.Cashier.CashierName</td>
                    <td class="text-center d-flex justify-content-around">
                        <a asp-action="Details" asp-route-id="@InvoiceHeader.Id" class="btn btn-info">Details</a>
                        <a asp-action="Update" asp-route-id="@InvoiceHeader.Id" class="btn btn-success">Edit</a>
                        <a asp-action="delete" asp-route-id="@InvoiceHeader.Id" onclick="confirmDelete()" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>

@section Scripts {
    <script>
        function confirmDelete() {
            var decision = confirm("are you sure");
            if (decision == false) {
                event.preventDefault();
            }
        }
    </script>
}