@model IEnumerable<City>

@{
    ViewData["Title"] = "Index";
}

<section class="d-flex justify-content-between">
    <h2>Cities</h2>
    <div>
        <a asp-action="Add" class="btn btn-outline-primary">Add</a>
    </div>
</section>
<hr />
<section>
    <table class="table table-striped table-bordered">
        <thead>
            <tr class="table-secondary">
                <th>City Name</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var city in Model)
            {
                <tr>
                    <td>@city.CityName</td>
                    <td class="text-center">
                        <a asp-action="Update" asp-route-id="@city.Id" class="btn btn-success me-2">Edit</a>
                        <a asp-action="delete" asp-route-id="@city.Id" onclick="confirmDelete()" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>

@section Scripts {
    <script>
        function confirmDelete() {
            var decision = confirm("are you sure");
            if (decision == false) {
                event.preventDefault();
            }
        }
    </script>
}
